model: nafnet

# Training
epochs: 50
batch_size: 8
learning_rate: 0.0002
input_size: 256

# Loss
loss: l1

# Scheduler
scheduler: cosine

# Data
train_csv: data/train.csv
val_csv: data/val.csv

# Checkpoints
checkpoint_dir: checkpoints/
